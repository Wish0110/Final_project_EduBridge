import React from 'react';
import MapChart from './MapChart'; // Import the MapChart component

const map = () => {
  return (
    <div>
      <h1>UK University MAP</h1>
      <MapChart /> {/* Render the MapChart component */}
    </div>
  );
};

export default map;
